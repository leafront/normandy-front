<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="renderer" content="webkit">
    <meta charset="utf-8">
    <title>车险投保 - 基本车辆信息</title>
    <link rel="stylesheet" href="/styles/insurance/index.css"/>
    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" />
</head>
<body>
<%include ../common/header.ejs%>
<div class="container clearfix">
    <% include ../common/aside_menu.ejs %>
    <div class="right-container fr" id="app">
        <div class="navigation">
            <a href="/" class="home_ico"></a>
            <em class="navigation_ico"></em>
            <span>基本车辆信息</span>
        </div>
        <div class="content" id="app">
            <div class="cont-title">
                <i></i>
                <span>基本车辆信息</span>
                <button class="list_btn" @click="orderAction">查看订单列表</button>
            </div>
            <div class="process_info">
               <ul class="process_list">
                 <li class="active">
                   <i>1</i>
                   <span>基本车辆信息</span>
                 </li>
                 <li>
                   <i>2</i>
                   <span>详细车辆信息</span>
                 </li>
                 <li>
                   <i>3</i>
                   <span>车险报价</span>
                 </li>
                 <li>
                   <i>4</i>
                   <span>配送信息</span>
                 </li>
                 <li>
                   <i>5</i>
                   <span>投保确认</span>
                 </li>
               </ul>
            </div>
            <div class="insurance_info">
                <div class="form-group">
                    <div class="form-group-rows form-group-ellipsis">
                        <label>行驶地区</label>
                        <div class="drop_menu drop-menu-city" :class="{'active':dropMenu == 1}">
                            <span @click.stop="selectMenu(1)">{{provinceName}}</span>
                            <ul class="drop_menu_list">
                                <li @click="selectProvince(item)" v-for="(item,index) in province"><a href="javascript:;">{{item.provinceName}}</a></li>
                            </ul>
                        </div>
                        <div class="drop_menu drop-menu-city" :class="{'active':dropMenu == 2}">
                            <span @click.stop="selectMenu(2)">{{cityName}}</span>
                            <ul class="drop_menu_list">
                                <li @click="selectCity(item)" v-for="(item,index) in cityList"><a href="javascript:;">{{item.cityName}}</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="form-group">
                    <div class="form-group-rows">
                        <label>车牌号</label>
                        <div class="drop_menu drop-menu-loan" :class="{'active': dropMenu == 3}">
                            <span @click.stop="selectMenu(3)">{{licenseName}}</span>
                            <ul class="drop_menu_list">
                              <li @click="selectLicense(item)" v-for="(item,index) in licensePlate"><a href="javascript:;">{{item}}</a></li>
                            </ul>
                        </div>
                        <input type="text" placeholder="车牌号码" class="user-ui-input" v-model.trim="licenseNumber">
                        <strong class="vehicle_checkbox">异响</strong>
                    </div>
                </div>
                <div class="form-group">
                    <div class="form-group-rows">
                        <label>车主姓名</label>
                        <input type="text" placeholder="请输入车主姓名" class="user-ui-input user-base-input" v-model.trim="ownerName">
                    </div>
                </div>

                <div class="insurance_submit">
                   <button class="btn_confirm" @click="actionInfo">立即获取报价</button>
                   <button class="btn_cancel">清空</button>
                </div>
            </div>
            <div class="cont-title">
                <i></i>
                <span>合作保险公司</span>
            </div>
            <div class="friend_link">
               <ul class="link_list clearfix">
                <%for (var i = 1; i <=8; i++){%>
                 <li><img src="/images/link_0<%-i%>.png"></li>
                <%}%>
               </ul>
            </div>
         </div>
        <% include ../common/footer.ejs %>
    </div>
</div>
<script type="text/javascript" src="/dist/scripts/insurance/index.js"></script>
</body>
</html>